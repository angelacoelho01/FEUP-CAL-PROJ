#ifndef SRC_UTILS_H
#define SRC_UTILS_H

#include <iostream>

// Some util functions to get the min an max between to values
namespace util {
    int min(int a, int b);

    int max(int a, int b);
}

#endif //SRC_UTILS_H
